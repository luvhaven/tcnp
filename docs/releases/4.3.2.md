# 4.3.2 — Live tracking map visibility

- Replace the full-map blurred empty state with a compact, non-interactive notice. Basemap, controls, and tile failure messages remain visible without shared locations or location permission.
- Ignore transparent error placeholders when measuring successful tile loads, preserving fallback-provider behavior.
- Detach retired tile listeners and reset map references on teardown.
- Validation: TypeScript passes; 18 regression tests pass; targeted ESLint has zero errors and two existing dependency warnings.
- This change is on the Next.js upgrade candidate branch, not deployed to production 4.2.2. Signed-in browser/device validation remains outstanding.
